[project]
name = "snr-strategy"
version = "1.0.0"
description = "4H SNR Retest Entry Strategy implementation"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.0",
    "pandas>=2.3.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.1"
]
